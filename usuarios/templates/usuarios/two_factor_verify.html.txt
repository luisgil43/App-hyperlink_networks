{% load static %}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Two-factor verification - HYPERLINK NETWORKS</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    * { box-sizing: border-box; }
    body {
      margin: 0; padding: 0;
      font-family: 'Segoe UI', sans-serif;
      background-color: #f4f6f8;
      display: flex; flex-direction: column;
      align-items: center; justify-content: center;
      min-height: 100vh;
    }
    .twofa-container {
      background-color: white;
      padding: 2rem;
      border-radius: 16px;
      box-shadow: 0 5px 20px rgba(0,0,0,0.08);
      max-width: 420px; width: 100%;
      text-align: center;
    }
    .twofa-container img {
      max-width: 220px;
      height: auto;
      margin-bottom: 1.5rem;
      background: #f8f8f8;
      padding: 12px;
      border-radius: 12px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }
    .twofa-container h2 {
      margin: 0 0 8px;
      font-size: 1.4rem;
      color: #1e73be; /* Azul corporativo */
      letter-spacing: 1.2px;
      font-weight: 700;
      text-transform: uppercase;
    }
    .twofa-container p.subtexto strong {
      color: #1e73be;
    }
    .twofa-container p.description {
      font-size: 0.9rem;
      color: #555;
      margin-bottom: 1.5rem;
    }
    .errorlist {
      color: red;
      list-style: none;
      padding: 0;
      margin-bottom: 10px;
      text-align: left;
    }
    .message-box {
      padding: 10px;
      border-radius: 8px;
      margin-bottom: 15px;
      font-size: 0.9rem;
      text-align: left;
      opacity: 1;
      transition: opacity 0.5s ease-out;
    }
    .message-box.error   { background-color: #fdecea; color: #d93025; }
    .message-box.success { background-color: #e6ffed; color: #188038; }
    .message-box.info    { background-color: #e8f0fe; color: #1a73e8; }
    .message-box.warning { background-color: #fff4e5; color: #c27803; }
    form { text-align: left; }
    form p { margin-bottom: 15px; }
    form label {
      font-weight: bold;
      display: block;
      margin-bottom: 5px;
      font-size: 0.9rem;
    }
    form input[type="text"] {
      width: 100%;
      padding: 10px;
      border: 2px solid #ccc;
      border-radius: 8px;
      font-size: 1rem;
    }
    .remember-row {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-top: 4px;
      margin-bottom: 10px;
    }
    .remember-row input[type="checkbox"] {
      width: 16px;
      height: 16px;
      cursor: pointer;
    }
    .remember-row label {
      margin: 0;
      font-weight: normal;
      font-size: 0.85rem;
      color: #555;
    }
    button {
      width: 100%;
      padding: 12px;
      background-color: #1e73be;
      color: white;
      border: none;
      border-radius: 8px;
      font-weight: bold;
      font-size: 1rem;
      cursor: pointer;
      transition: background-color 0.3s;
      margin-top: 5px;
    }
    button:hover { background-color: #155a96; }
    .helper-text {
      font-size: 0.8rem;
      color: #777;
      margin-top: 4px;
    }
    .footer {
      text-align: center;
      font-size: 0.75rem;
      color: #999;
      margin-top: 8px;
    }
    .footer img {
      height: 60px;
      vertical-align: middle;
      margin-left: 0;
    }
    @media (max-width: 500px) {
      .twofa-container { padding: 1.5rem; margin: 0 1rem; }
      .twofa-container img { width: 120px; }
    }
  </style>
</head>
<body>

  <div class="twofa-container">
    <!-- Logo principal -->
    <img src="{% static 'images/logoh.png' %}" alt="Hyperlink Networks Logo">

    <!-- Título y subtítulo -->
    <h2>TWO-FACTOR VERIFICATION</h2>
    <p class="subtexto">Hyperlink Networks Platform</p>

    <!-- Mensajes del sistema (éxito, error, info, warning) -->
    {% if messages %}
      {% for message in messages %}
        <div class="message-box {{ message.tags }}">
          {{ message }}
        </div>
      {% endfor %}
    {% endif %}

    <!-- Descripción -->
    <p class="description">
      Hi <strong>{{ user.get_full_name|default:user.username }}</strong>. Please enter the verification code from your authentication app to continue.
    </p>

    <!-- Formulario de verificación 2FA -->
    <form method="post">
      {% csrf_token %}
      <p>
        <label for="code">Verification code</label>
        <input type="text"
               id="code"
               name="code"
               autocomplete="one-time-code"
               placeholder="123 456"
               required>
        <span class="helper-text">
          Enter the 6-digit code currently displayed in your authentication app (for example Google Authenticator or Authy).
        </span>
      </p>

      <div class="remember-row">
        <input type="checkbox" id="remember_device" name="remember_device">
        <label for="remember_device">
          Remember this device for 30 days
        </label>
      </div>

      <button type="submit">Verify and continue</button>
    </form>

    <p class="helper-text" style="margin-top: 12px;">
      If you closed this page or changed your device, please sign in again from the login page.
    </p>
  </div>

  <!-- Footer con logo de Planix -->
  <div class="footer">
    Developed by
    <img src="{% static 'images/planixl.png' %}" alt="Planix">
  </div>

  <!-- JS para ocultar mensajes después de unos segundos -->
  <script>
    setTimeout(() => {
      document.querySelectorAll('.message-box').forEach(el => {
        el.style.opacity = '0';
        setTimeout(() => el.remove(), 500);
      });
    }, 4000);
  </script>

</body>
</html>