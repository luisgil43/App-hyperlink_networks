# ── Python / Django ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.so
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.pytype/
.pyre/
.coverage
htmlcov/

# Bases SQLite (incluye WAL/SHM/journal)
*.sqlite
*.sqlite3
*.sqlite3-*
*.sqlite-journal
db.sqlite3
db.sqlite3-*
db.sqlite3-wal
db.sqlite3-shm

# Archivos de entorno (NO subir nunca)
.env
.env.*
!.env.example
.envrc

# Certificados / claves (NO subir nunca)
*.pfx
*.p12
*.pem
*.key
*.cer
*.crt
*.der
# ⚠️ OJO: ignorar *.xml global puede ocultarte XML legítimos
# Si es por SII, mejor ignora una carpeta específica:
# /sii_xml/*.xml

# Logs / temporales
*.log
*.tmp
*.bak
*.swp
*.swo
*.DS_Store

# ── Django static/media (artefactos locales) ─────────────────────
# collectstatic (salida)
staticfiles/
# uploads locales
media/
# si quieres mantener el directorio vacío en repo:
# !media/.gitkeep

# ── Entorno virtual ─────────────────────────────────────────────
.venv/
.venv*/
venv/
env/
.venv312/

# ── Editor/IDE ──────────────────────────────────────────────────
.vscode/
.idea/

# ── Node / build front (si usas Tailwind/Vite/React) ────────────
node_modules/
dist/
build/
.parcel-cache/
.pnp.*