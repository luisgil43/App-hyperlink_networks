{% extends "dashboard_admin/base.html" %}
{% block dashboard_content %}
<div class="p-6">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-bold text-gray-800">Contratos de Trabajo</h1>
    <a href="{% url 'rrhh:crear_contrato' %}" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">
      ➕ Añadir Contrato de Trabajo
    </a>
  </div>

  <div class="overflow-x-auto bg-white rounded-lg shadow">
    <table id="tabla-contratos" class="min-w-full divide-y divide-gray-200">
      <thead class="bg-gray-100">
        <tr>
          <th class="px-4 py-3 text-left text-sm font-semibold text-gray-700">ID</th>
          <th class="px-4 py-3 text-left text-sm font-semibold text-gray-700">Nombre y Apellido</th>
          <th class="px-4 py-3 text-left text-sm font-semibold text-gray-700">Fecha de Inicio</th>
          <th class="px-4 py-3 text-left text-sm font-semibold text-gray-700">Fecha de Término</th>
          <th class="px-4 py-3 text-left text-sm font-semibold text-gray-700">Contrato Adjunto</th>
          <th class="px-4 py-3 text-left text-sm font-semibold text-gray-700">Acciones</th>
        </tr>
        <tr>
          <th></th>
          <th>
            <select class="filtro px-2 py-1 w-full text-sm border">
              <option value="">Todos</option>
              {% for nombre in nombres %}
              <option>{{ nombre }}</option>
              {% endfor %}
            </select>
          </th>
          <th>
            <select class="filtro px-2 py-1 w-full text-sm border">
              <option value="">Todos</option>
              {% for fecha in fechas_inicio %}
              <option>{{ fecha }}</option>
              {% endfor %}
            </select>
          </th>
          <th>
            <select class="filtro px-2 py-1 w-full text-sm border">
              <option value="">Todos</option>
              {% for fecha in fechas_termino %}
              <option>{{ fecha }}</option>
              {% endfor %}
            </select>
          </th>
          <th></th>
          <th></th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-100 bg-white">
        {% for contrato in contratos %}
        <tr class="hover:bg-gray-50 transition">
          <td class="px-4 py-3 text-sm text-gray-900">{{ contrato.id }}</td>
          <td class="px-4 py-3 text-sm text-gray-900">{{ contrato.tecnico.get_full_name }}</td>
          <td class="px-4 py-3 text-sm text-gray-900">{{ contrato.fecha_inicio }}</td>
          <td class="px-4 py-3 text-sm text-gray-900">
  {% if contrato.fecha_termino %}
    {{ contrato.fecha_termino }}
  {% else %}
    Indefinido
  {% endif %}
</td>
          <td class="px-4 py-3 text-sm text-gray-900">
            {% if contrato.archivo %}
             <a href="{% url 'rrhh:ver_contrato' contrato.id %}" class="text-blue-600 hover:underline" target="_blank">
  Ver contrato
</a>
            {% else %}
              <span class="text-gray-400 italic">No disponible</span>
            {% endif %}
          </td>
          <td class="px-4 py-3 text-sm flex gap-3">
            <a href="{% url 'rrhh:editar_contrato' contrato.id %}" class="text-blue-600 hover:underline font-medium">Editar</a>
            <a href="{% url 'rrhh:eliminar_contrato' contrato.id %}" class="text-red-600 hover:underline font-medium">Eliminar</a>
          </td>
        </tr>
        {% empty %}
        <tr>
          <td colspan="6" class="px-4 py-6 text-center text-gray-500">No se encontraron contratos.</td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
</div>

<script>
  const filtros = document.querySelectorAll(".filtro");
  const tabla = document.getElementById("tabla-contratos").getElementsByTagName("tbody")[0];

  filtros.forEach((select, colIndex) => {
    select.addEventListener("change", () => {
      const valores = Array.from(filtros).map(s => s.value.trim().toLowerCase());

      Array.from(tabla.rows).forEach(row => {
        let visible = true;

        valores.forEach((filtro, i) => {
          if (filtro !== "" && filtro !== "todos") {
            const cellText = row.cells[i + 1].textContent.replace(/\s+/g, " ").trim().toLowerCase();
            if (!cellText.includes(filtro)) {
              visible = false;
            }
          }
        });

        row.style.display = visible ? "" : "none";
      });
    });
  });
</script>
{% endblock %}
